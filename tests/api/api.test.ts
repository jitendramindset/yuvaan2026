import { describe, it } from "vitest";

describe("api — integration tests", () => {
  it.todo("GET /api/nodes returns node list");
  it.todo("POST /api/nodes creates a node");
  it.todo("GET /api/sync/:id returns sync state");
});
