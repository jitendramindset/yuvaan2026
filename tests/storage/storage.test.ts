import { describe, it } from "vitest";

describe("storage — leveldb / faiss integration tests", () => {
  it.todo("batch_writer writes and reads back a key");
  it.todo("vector_index indexes and searches embeddings");
});
