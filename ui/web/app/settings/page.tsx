"use client";
import { SettingsScreenContent } from "@/components/screens/SettingsScreenContent";

export default function SettingsPage() {
  return (
    <div style={{ height: "calc(100vh - 3.5rem)", overflow: "auto" }}>
      <SettingsScreenContent />
    </div>
  );
}
